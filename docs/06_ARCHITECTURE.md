# 06. 完成形の設計図（どの回で、何が生まれるか）

このファイルは**制作側だけが見る設計図**です。動画では、ここまで先の話はしません。
ただし、**このとおりに作れば、30回の途中で作り直しが起きない**ことを保証するために、先に決めておきます。

---

## 1. パッケージは作りません

初心者に `package` の説明をすると、フォルダとファイルの関係でつまずきます。
そこで、**すべてのクラスを `src` の直下（デフォルトパッケージ）に置きます。**

- クラスは全部で **34個**。1つのフォルダに34個並ぶのは多いですが、`package` の説明をするより安全です
- Eclipse の左側のツリーは名前順に並ぶので、探すのに困りません
- 第30回の「これからの一歩」で、「本当は `package` で分けると整理できます」と名前だけ紹介します

---

## 2. クラス一覧（全34クラス）

| # | クラス名 | 役わり | 生まれる回 | 手を入れる回 |
| --- | --- | --- | --- | --- |
| 1 | `Main` | いちばん最初に動く所 | 2 | 4 |
| 2 | `GameWindow` | ウィンドウを開く | 4 | 5 |
| 3 | `GamePanel` | 絵を描く／ゲームループ／全体のまとめ役 | 5 | 6,7,8,9,11,12,14,15,17,19,29 |
| 4 | `KeyHandler` | キーの押し・はなしを覚える | 7 | 17 |
| 5 | `TileSet` | タイル画像と、その性質を持つ | 9 | 12 |
| 6 | `TileMap` | マップの数字の表と、描く処理 | 9 | 10,11,26 |
| 7 | `Camera` | どこを映すかを決める | 11 | ─ |
| 8 | `Sprite` | 画像の切り出しとコマ送り | 13 | ─ |
| 9 | `Entity` | 動くもの共通の土台 | 14 | 15 |
| 10 | `Player` | 主人公 | 14 | 22,24,25,27 |
| 11 | `Npc` | 町の人 | 15 | 18,26 |
| 12 | `FontRenderer` | フォントの読みこみと文字描き | 16 | 17 |
| 13 | `GameWindowBox` | まどの枠を描く（黒＋白い二重枠） | 16 | ─ |
| 14 | `MessageWindow` | 会話まど（1文字ずつ／ページ送り） | 16 | 17 |
| 15 | `SceneType` （enum） | いまどの画面か | 17 | 19,29 |
| 16 | `Flags` | イベントの「やった／まだ」 | 18 | 26,27 |
| 17 | `ScriptEngine` | 会話の台本を読んで、条件で選ぶ | 18 | 26 |
| 18 | `Enemy` | 敵1体ぶんの数字と絵 | 19 | 23,29 |
| 19 | `EnemyTable` | `enemies.csv` を読んで持っておく | 19 | 23,29 |
| 20 | `BattleScene` | 戦闘画面のまとめ役 | 19 | 20,21,22,23,24,25 |
| 21 | `CommandMenu` | コマンドのカーソル | 20 | 25,26,29 |
| 22 | `BattleState` （enum） | 戦闘のいまの様子 | 21 | 23 |
| 23 | `DamageCalc` | ダメージの計算だけをする | 22 | 23,25 |
| 24 | `EnemyAi` | 敵の行動を確率の表から選ぶ | 23 | ─ |
| 25 | `Spell` | じゅもん1つぶん | 23 | ─ |
| 26 | `SpellTable` | `spells.csv` を読む | 23 | ─ |
| 27 | `LevelTable` | `levels.csv` を読む | 24 | ─ |
| 28 | `ItemType` （enum） | どうぐ／ぶき／よろい／たて | 25 | ─ |
| 29 | `Item` | アイテム1つぶん | 25 | ─ |
| 30 | `ItemTable` | `items.csv` を読む | 25 | ─ |
| 31 | `Inventory` | 持ち物（10個まで）と装備 | 25 | 27 |
| 32 | `MapManager` | マップの切りかえと出入り口 | 26 | 27 |
| 33 | `SaveManager` | セーブとロード | 27 | ─ |
| 34 | `SoundManager` | BGM と 効果音 | 28 | ─ |

> `TitleScene` / `GameOverScene` / `EndingScene`（第29回）は、
> **クラスにせず `GamePanel` の中のメソッドとして書きます**。
> それぞれ20〜30行と短く、クラスを3つ増やすほどではないからです。

---

## 3. 「まとめ役」の構造

初心者にとって、**どこから読めばよいか分かること**が何より大切です。
そこで、次の3つだけを「まとめ役」とし、それ以外は道具に徹します。

```
Main
 └ GameWindow        … ウィンドウを開くだけ
     └ GamePanel     ★まとめ役1: ゲームループと、画面の切りかえ
         ├ FIELD   のとき → TileMap / Camera / Player / Npc / MessageWindow
         ├ BATTLE  のとき → BattleScene   ★まとめ役2: 戦闘のすべて
         ├ MENU    のとき → CommandMenu / Inventory
         └ TITLE / GAMEOVER / ENDING のとき → GamePanel 内のメソッド
```

**決まりごと**
- `GamePanel` の `update()` と `draw()` は、`switch (sceneType)` で分ける。それ以上のことはしない
- 計算は必ず、それ専用のクラス（`DamageCalc` など）に置く。`BattleScene` の中に計算式を書かない
- データを読む処理は必ず `〜Table` という名前のクラスに置く

---

## 4. データファイルの形（全部あわせて先に決めておく）

すべて **UTF-8のテキストファイル**、区切りは **カンマ**、`#` で始まる行は説明として読み飛ばします。

### `res/data/tiles.csv`（第12回）
```
# 番号,なまえ,通れるか(1=通れる/0=通れない),エンカウントするか(1/0)
0,くさ,1,1
1,みず,0,0
2,き,0,0
3,かべ,0,0
4,みち,1,1
5,ゆか,1,0
6,かいだん,1,0
```

### `res/maps/*.txt`（第10回）
```
# 1行目: よこのマス数 たてのマス数
40 40
0 0 0 1 1 0 0 ...
0 2 0 1 1 0 0 ...
```

### `res/data/enemies.csv`（第19回に作り、第23・29回で列を足す）
```
# id,なまえ,HP,MP,ちから,みのまもり,すばやさ,経験値,ゴールド,画像
# ,,,,,,,,,たたかう%,じゅもん%,ぼうぎょ%,なにもしない%,じゅもんid,にげられるか
1,スライム,3,0,5,3,2,1,1,enemy_slime.png,100,0,0,0,-1,1
5,まおう,130,60,120,80,60,0,0,enemy_boss.png,50,40,10,0,2,0
```
> **第19回では 10列だけ**にして、第23回で行動の割合の4列とじゅもんid、
> 第29回で「にげられるか」を足します。**列を足すだけで、既存の列は動かしません。**

### `res/data/spells.csv`（第23回）
```
# id,なまえ,消費MP,種類(1=こうげき/2=かいふく),効果の数値,使えるレベル
1,ホイミ,4,2,30,3
2,ギラ,2,1,12,4
```

### `res/data/levels.csv`（第24回）
```
# レベル,必要経験値,HP,MP,ちから,みのまもり,すばやさ
1,0,15,0,4,2,4
2,7,22,0,5,4,4
```

### `res/data/items.csv`（第25回）
```
# id,なまえ,種類,値段,効果の数値,せつめい
1,やくそう,どうぐ,24,30,HPを 30 かいふくする
```

### `res/data/warps.csv`（第26回）
```
# 今のマップ,列,行,行き先マップ,行き先の列,行き先の行
overworld,12,8,town1,10,14
```

### `res/scripts/*.txt`（第18回）
```
# id,条件フラグ(-はなし),本文(\ で改ページ)
king,-,おお ゆうしゃよ！\まおうを たおしてくれ。
king,KING_TALKED,たのんだぞ ゆうしゃよ。
```

### セーブデータ（第27回）
`ホームフォルダ/.pure-java-rpg/save1.properties`
```
version=1
map=town1
col=10
row=14
level=5
exp=120
gold=340
hp=38
mp=12
items=1,1,2
equipWeapon=2
equipArmor=3
flags=KING_TALKED,GOT_KEY
```

---

## 5. 決めておいた数字（コードの定数）

`GamePanel` の上のほうに、**まとめて**書きます。

```java
// ---- 画面の決まり ----
public static final int TILE_SIZE   = 16;   // 1マスの大きさ（ピクセル）
public static final int SCREEN_COL  = 16;   // 画面によこに入るマス数
public static final int SCREEN_ROW  = 15;   // 画面にたてに入るマス数
public static final int GAME_WIDTH  = 256;  // 16 × 16
public static final int GAME_HEIGHT = 240;  // 16 × 15
public static final int SCALE       = 3;    // 何倍に引きのばすか
public static final int FPS         = 60;   // 1秒間に描く回数
```

**この形にした理由**: 数字が1か所にまとまっていると、「画面を大きくしたい」というときに
1行直せば済みます。第5回で、この考え方を教えます。

---

## 6. 作らないと決めたもの（大事な線引き）

「あったほうが良いけれど、15分×30回には入らない」と判断したものです。
第30回の「これからの一歩」で、名前だけ紹介します。

| 作らないもの | 理由 |
| --- | --- |
| 仲間（パーティ4人） | 戦闘の作りが一気に難しくなる。ドラクエ1も1人 |
| 敵が複数体 | 同じ理由。ターゲットを選ぶ処理が増える |
| ふっかつのじゅもん | 昔の技術。今どきのファイル保存で十分（第27回でコラムとして紹介） |
| 16色パレットの再現 | 見た目のためだけに、難しい話が増える |
| デルタタイム（動きの時間補正） | 60FPS固定で十分。中学までの算数を超える |
| セーブデータの暗号化 | 説明に時間がかかる。第27回の宿題で「書きかえられること」を体験してもらう |
| 音の同時再生数の管理 | `Clip` をいくつか持っておくだけで足りる |
| マップエディタ | 表計算ソフトでCSVを書けば十分（第10回の宿題で紹介） |
| セリフの自動改行（漢字まじり） | ひらがな・カタカナだけなら、文字数で数えれば足りる |

---

## 7. コードの量の見積もり

| 章 | 回 | 新しく書く行数（合計） |
| --- | --- | --- |
| 第0章 | 1〜3 | 約 30行 |
| 第1章 | 4〜7 | 約 180行 |
| 第2章 | 8〜12 | 約 280行 |
| 第3章 | 13〜15 | 約 200行 |
| 第4章 | 16〜18 | 約 220行 |
| 第5章 | 19〜24 | 約 400行 |
| 第6章 | 25〜26 | 約 180行 |
| 第7章 | 27〜30 | 約 200行 |
| **合計** | | **約 1,690行** |

- 1回あたり平均 **約56行**。ルールの「60行まで」に収まっています
- いちばん多いのは第22回（ダメージ計算）と第26回（町と店）で、それぞれ約70行。
  **この2回は、収録前に写経部分を測ってください**（あふれるなら、店を第26回の宿題側に寄せる）

---

## 8. 動作を確かめる環境

| 項目 | 内容 |
| --- | --- |
| JDK | 25 |
| 確かめるOS | Windows 11 / macOS |
| Eclipse | JDK 25 に対応したバージョン |
| 確かめ方 | 各回のタグ（`ep01`〜`ep30`）を新しいフォルダに取り出し、手順どおりにやって動くか |

> **収録前に必ず**: `git tag ep○○` を打ったあと、**まっさらな環境で1度動かす**。
> 「自分のパソコンでだけ動く」を防ぐためです。
